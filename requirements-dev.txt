# Test dependencies, pinned to the versions the suite was verified with.
# The engine itself needs only the standard library; this file is for running
# the tests (CI installs it). `pip install -r requirements-dev.txt`
pytest==9.1.1
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
colorama==0.4.6; sys_platform == "win32"

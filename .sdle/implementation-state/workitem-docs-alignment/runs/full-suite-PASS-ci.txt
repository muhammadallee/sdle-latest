=== Full test suite - PASS (GitHub Actions) ===
candidate : 7477e9f317673f52ea42c8de02136ab376535c1e
branch    : docs/alignment-binding-and-isolation
event     : push
run       : https://github.com/muhammadallee/sdle-latest/actions/runs/35727873456
started   : 2026-09-22T12:33:14Z
finished  : 2026-09-22T12:56:34Z
status    : completed / success

RESULT: PASS on all four matrix jobs. This resolves OPEN-03.

| job                            | conclusion |
|--------------------------------|------------|
| ubuntu-latest  / Python 3.11   | success    |
| ubuntu-latest  / Python 3.13   | success    |
| windows-latest / Python 3.11   | success    |
| windows-latest / Python 3.13   | success    |

Each job runs `python scripts/sdle.py lint-skill` and the full `python -m pytest
-q` suite. The only non-success steps are the two platform-inappropriate
launcher checks, skipped by design: the POSIX launcher check is skipped on
Windows and the PowerShell launcher check on Linux. Every job runs the other.

This is stronger evidence than the local foreground run that was the
alternative: that would have covered one combination (Windows, Python 3.13) on a
memory-constrained machine, where CI covers four on clean runners.

The step logs need an authenticated request, so the per-job passed-test count is
not recorded here; the run's own page carries it. What is recorded is what the
API reports without authentication: four jobs, all `success`, at this exact sha.

The three local attempts that produced no result are preserved below for the
record.

------------------------------------------------------------------------------
=== Full test suite - NOT_RUN ===
final candidate : a669b03 (the attempts below predate it; see OPEN-03)
command         : python -m pytest tests/ -q -p no:randomly
date            : 2026-09-22

RESULT: NOT_RUN, after three attempts. No attempt was a test failure.

1. Candidate b83cf4a. Started before the last six edits of the round-1 pass, so
   it was measuring a tree that changed underneath it. Killed deliberately
   rather than quoted. Its log is empty; its "exit code 0" notification is the
   kill, not a pass.
2. Candidate b83cf4a, re-run against the committed tree. Stopped by the harness
   at roughly 4% because the system was critically low on memory while the
   session was idle.
3. Candidate 44c5d24, re-run at the owner's explicit direction. Stopped by the
   same harness memory reaper at 11%. Partial output below.

Attempts 2 and 3 were stopped by Claude Code's background-shell memory reaper,
which is a property of this machine's free memory at the time, not of the suite.
The harness asks that such a command not be restarted unattended.

No failure was observed in the 11% that ran on attempt 3 (zero F or E markers).
That is not evidence of a pass and is not recorded as one.

Two options remain untried and both need the owner:
  - run it in the FOREGROUND, which is not subject to the background-shell
    reaper, accepting that it blocks the session for roughly 38 minutes; or
  - restart Claude Code with CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP=1 in its
    environment (it cannot be set from inside a shell command) and re-run in the
    background.

Checks that DID complete against this candidate are recorded separately and all
passed: lint-skill; test_dry_run_contracts.py (118);
test_units_documented_commands.py + test_units_invariants.py (935); the guide
replay; the binding scenarios; the ADR-012 refusal probe; the
deleted-bound-source probe.

Partial output of attempt 3 follows.

candidate: 44c5d24 (clean apart from the owner's untracked file)
started:   2026-09-22T10:07:42Z
........................................................................ [  2%]
........................................................................ [  4%]
........................................................................ [  6%]
........................................................................ [  9%]
........................................................................ [ 11%]
......................
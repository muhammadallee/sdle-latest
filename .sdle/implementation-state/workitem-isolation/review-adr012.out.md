1. High — [scripts/sdle.py:4525](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:4525), [scripts/sdle.py:5242](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:5242), [scripts/sdle.py:5074](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:5074). A missing source can be made fresh by reassessing it. `requirements_sources` records a missing file with `sha256: null`, and `governance assess` accepts that as a new baseline. Sequence: bind A, assess, initialize, delete A, observe stale, rerun `governance assess` with a passing proposal, then advance. The new record contains null; freshness computes the same null and reports fresh. The null cannot collide with a real SHA-256—the encoded values are `"None"` versus hexadecimal—but it can become the recorded baseline, which is the actual defect. Fix assessment-time capture to refuse `requirements_source_missing`; reserve null only for freshness comparison against an earlier non-null record.

2. High — [scripts/sdle.py:5085](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:5085). Pre-ADR governance records bypass the mandatory binding. The parent implementation wrote governance version 3 without `bindingDigest`; this code treats a missing `bindingDigest` as “do not compare.” An already-initialized WorkItem can therefore upgrade, remain completely unbound—or bind a wholly different set—and advance while its old source files remain unchanged. This violates the no-implicit-default requirement. Fix by treating a missing `bindingDigest` as stale under ADR-012, or bumping the governance record version and requiring reassessment.

3. High — [scripts/sdle.py:4463](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:4463), [scripts/sdle.py:4498](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:4498), [scripts/sdle.py:3557](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:3557). The engine-written binding is not integrity-validated when read. Only the document type, `sources` being a list, and version are checked. Individual entries, non-emptiness, duplicates, `primary`, `workitem`, and the stored digest are trusted; `validate` never parses the binding. A merge or skipped-hook edit can insert `../../outside.md` or an absolute Windows path, after which governance and project-name inference read outside the repository. Non-string entries can instead produce an uncaught traceback rather than `requirements_binding_invalid`. Fix with one complete binding validator that rechecks the schema, source syntax/containment, primary membership, uniqueness, WorkItem identity, and recomputed digest, and invoke it from `validate`.

4. High — [scripts/sdle.py:4428](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:4428), [scripts/sdle.py:4525](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:4525), [scripts/sdle.py:1599](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:1599). Symlink containment is checked only at bind time. Bind a symlink while it targets an in-repository file, then retarget it outside the repository. `preflight`, `requirements show`, governance assessment/freshness, and project-name inference subsequently follow it without containment validation. If the outside file has identical contents, freshness can even remain true. Fix by resolving and containment-checking every bound path at every consumer read, not just during `requirements bind`.

5. Medium — [.claude/commands/sdle-start.md:48](D:/Learning/AI/sdle-git-repo/sdle-latest/.claude/commands/sdle-start.md:48), [.claude/commands/sdle-start.md:57](D:/Learning/AI/sdle-git-repo/sdle-latest/.claude/commands/sdle-start.md:57), [.claude/skills/sdle/modules/phase-execution.md:50](D:/Learning/AI/sdle-git-repo/sdle-latest/.claude/skills/sdle/modules/phase-execution.md:50), [.claude/skills/sdle/modules/phase-execution.md:63](D:/Learning/AI/sdle-git-repo/sdle-latest/.claude/skills/sdle/modules/phase-execution.md:63). The operational prompt layer does not consistently consume the binding. The start command jumps from preflight to governance proposal without explicitly showing and scanning the bound list first. Discovery and impact analysis explicitly read and scan every document under `requirements/`. Thus WorkItem A can bind `a.md`, while unrelated `b.md` is still incorporated into its discovery or impact analysis even though freshness correctly ignores it. Fix every prompt path to obtain the list from `requirements show`, then read and scan only those paths, before proposal generation and again where later phases need requirements.

6. Medium — [scripts/sdle.py:4411](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:4411), [scripts/sdle.py:4482](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:4482). Windows aliases evade duplicate and source validation. On this checkout, `_binding_source` accepts `requirements/todo-api.md::$DATA`; Windows resolves that to the ordinary file, while the stored lexical path remains distinct. A trailing-dot spelling behaves similarly, and an existing non-default ADS would bind data that Git does not version. Consequently the same file can be bound twice, and case-alias spelling of `--primary` can be rejected by the later case-sensitive membership check. UNC and `\\?\` absolute prefixes are rejected, and reserved devices fail `is_file`, but ADS/trailing-dot aliases are not. Fix by rejecting Windows-reserved syntax—including colons in components and trailing dots/spaces—and comparing source identities with canonical paths or `samefile`.

7. Medium — [scripts/sdle.py:5239](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:5239). A refused unbound assessment still writes an empty evidence file. `reserve_evidence` runs before `requirements_sources`, which is where `requirements_unbound` is raised. Running `governance assess` before binding therefore creates `.sdle/evidence/governance-…json` and only then refuses. This breaks refusal atomicity and leaves an unexplained artifact. Validate and hash the binding before reserving the evidence filename.

8. Low — [scripts/sdle.py:1596](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:1596), [scripts/sdle.py:8492](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:8492). Primary inference can select a document that was not primary. If the selected primary has no level-one heading, `infer_project_name` scans every secondary source. With a headingless product document and `# PCI DSS` in a secondary regulatory document, the project becomes “PCI DSS” instead of falling back to the WorkItem title. Separately, `--all-current` alphabetically selects the primary, so `00-regulatory.md` can produce the same wrong result without an explicit choice. Fix inference to inspect only `primary` and then use the existing title/root fallback; for multiple sources, requiring `--primary` would remove the arbitrary alphabetical choice.

9. Low — [.claude/skills/sdle/SKILL.md:275](D:/Learning/AI/sdle-git-repo/sdle-latest/.claude/skills/sdle/SKILL.md:275), [.claude/skills/sdle/SKILL.md:289](D:/Learning/AI/sdle-git-repo/sdle-latest/.claude/skills/sdle/SKILL.md:289), [.claude/skills/sdle/SKILL.md:403](D:/Learning/AI/sdle-git-repo/sdle-latest/.claude/skills/sdle/SKILL.md:403), [docs/SDLE-Reference-Guide.md:512](D:/Learning/AI/sdle-git-repo/sdle-latest/docs/SDLE-Reference-Guide.md:512). The retired reason is gone from executable code, but live guidance still describes its old semantics: preflight depends on an absent/empty directory, any change under `requirements/` stales governance, and deleting the directory mid-workflow merely warns and continues. The reference guide also says every file is scanned and project naming comes from the directory. These statements produce behavior contrary to ADR-012 even without mentioning the old reason string. Fix the remaining prompt and reference material to say “bound sources” consistently and to describe deletion as stale/missing.

10. Low — [scripts/sdle.py:4453](D:/Learning/AI/sdle-git-repo/sdle-latest/scripts/sdle.py:4453). `binding_digest` has ambiguous framing on POSIX filesystems. Valid source sets `["a\nb", "c"]` and `["a", "b\nc"]` both hash the text `a\nb\nc`; rebinding between them is therefore invisible if the old files remain unchanged. Fix by hashing canonical JSON or length-prefixed strings, or reject control characters in source names.

For test adequacy, I found no specific weakening caused by the shared `create_wi` adaptation: tests that depend on a runtime-less WorkItem use `create_wi_unbound`, and the write-call and command-surface snapshots are numerically correct—26 `write_atomic` occurrences and 74 exact command names. The missing coverage is instead visible in the defects above: there are no tests for delete-then-reassess, legacy records without `bindingDigest`, malformed bindings, symlink retargeting, Windows aliases, or the prompt-level discovery/impact paths. I did not run pytest because the suite creates temporary files and this review was constrained to read-only inspection.

No additional defect was found in `--all-current` snapshot enumeration itself, the manifest exclusions, drift machinery, or hook fencing beyond the validation and prompt issues above.

No: the implementation is not correct and complete for ADR-012 as written. The single most valuable change would be one authoritative “read and validate the current binding” path, used by every consumer, which refuses missing sources during assessment and rechecks schema and filesystem containment on every read.